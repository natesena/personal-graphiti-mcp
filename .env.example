# Neo4j connection (matches docker-compose defaults)
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=demodemo
NEO4J_PORT=NEO4J_PORT=7687

# LLM settings for Ollama
OPENAI_API_KEY=ollama
MODEL_NAME=llama3
EMBEDDER_MODEL_NAME=nomic-embed-text
OPENAI_BASE_URL=http://host.docker.internal:11434/v1

# Optional: You can set a group ID to namespace your graph data
# GROUP_ID=default

# Optional: LLM temperature (0.0-2.0, lower is more deterministic)
# LLM_TEMPERATURE=0.7